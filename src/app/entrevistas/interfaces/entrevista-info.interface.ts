export interface EntrevistaInfo {
  postulante: {
      nombres: string;
      apellidos: string;
  };
  rol: {
      detalle: string;
  };
  celula: {
      nombre: string;
  };
  fecha_entrevista: string;
}
